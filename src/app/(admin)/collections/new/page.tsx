import CollectionForm from "@/components/admin/collections/CollectionForm";

/**
 * Composant de la page de création d'une nouvelle collection dans la section d'administration.
 * Cette page affiche simplement le formulaire de création de collection.
 */
export default function CreateCollection() {
  return <CollectionForm />; // Rend le formulaire de création/édition de collection
}
