import ProductForm from "@/components/admin/articles/ProductForm";

/**
 * Composant de la page de création d'un nouvel article (produit) dans la section d'administration.
 * Cette page affiche simplement le formulaire de création de produit.
 */
export default function CreateArticle() {
  return <ProductForm />; // Rend le formulaire de création/édition de produit
}
